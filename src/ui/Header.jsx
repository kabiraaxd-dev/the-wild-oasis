import styled from "styled-components"
const StyledHeader = styled.header`
background-color: var(--color-brand-700);
color: var(--color-white);
`
function Header() {
    return (
        <StyledHeader>StyledHeader</StyledHeader>
    )
}

export default Header